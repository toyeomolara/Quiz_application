<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard</title>
    
</head>
<body>
    <h1>Leaderboard</h1>
    <ul>
        <% topScorers.forEach((scorer, index) => { %>
            <li><%= index + 1 %>. User <%= scorer._id %>: <%= scorer.totalCorrectAnswers %> correct answers</li>
        <% }); %>
    </ul>
</body>
</html>